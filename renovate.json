{
  "customManagers": [
    {
      "fileMatch": ["^Dockerfile$"],
      "matchString": "ENV .*?_VERSION=(?<currentValue>.*) # (?<datasource>.*?)/(?<depName>.*)\\s(versioning=)?(?<versioning>.*?)?\\s*"
    }
  ]
}
